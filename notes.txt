sshd_config     -> /etc/ssh/sshd_config

apt
apt.conf        -> /etc/apt/apt.conf
sources.list    -> /etc/apt/sources.list

~/
        .bashrc
        .profile

xmodmap (xmodmap-uk-qwerty|xmodmap-us-qwerty)

# add user to sudoers file
su root
adduser jan sudo
reboot

graka: zie graka.txt + xorg.conf
